# ╔═══ BASELINE — 由 letters/Template/.gitignore 同步，勿在本區編輯 ═══╗
# 要改共用規則：改基線檔，再跑 sync_letters_gitignore.py（--check 只報漂移）。
# 自訂規則寫在檔尾「本 persona 自訂」區 —— 那一區同步工具不動。
# baseline_sha256: d3e78f72269a9aeeaa37c8f2cb602cfa478758b0282953417f01779a1e5832bd
### 這一區是什麼
# **letters repo 的 `.gitignore` 基線（baseline）。**
# 唯一真相源住在 `letters/Template/.gitignore`；其他 persona 的檔由
# `sync_letters_gitignore.py` 同步 —— BASELINE 區塊是基線的複製，
# 而 BASELINE END 之後的「本 persona 自訂」區塊同步工具不動。
#
# 要改共用規則 → 改本檔，再跑：
#   python <UCL_Core>/Tools~/AgentCommands/sync_letters_gitignore.py            # 同步
#   python <UCL_Core>/Tools~/AgentCommands/sync_letters_gitignore.py --check    # 只報漂移
#
# 為什麼要有基線：2026-08-18 實掃八個 letters repo，共用的只有 7 條，
# 其餘是**各自長出來的逐檔清單** —— `_streamwatch_observe.md` 只有 4 個 repo 擋、
# `_freetime_partners.md` 只有 1 個。那份清單天生會落後：新增一支 Cmd／一個 step 就漏一個，
# 而漏掉的症狀是「檔案開始出現在 git status 裡」，看起來跟「我今天寫了東西」一模一樣。

### Windows 垃圾檔
Thumbs.db
[Dd]esktop.ini
$RECYCLE.BIN/
*.lnk

### Cmd 回傳檔 —— **目錄層規則**（2026-08-18 全數遷入 cmd/ 之後的唯一寫法）
# 回傳檔是 transient：每跑一次 Cmd 就重生、手改無效、無存檔價值。
# ⛔ 而且部分回傳檔**含憑證**：`cmd/wake_brief.md` 的 §0 身分卡帶 session_token
#    （失憶救援憑證）與個人信箱，而 letters remote 可能是公開的 —— history 刪不掉。
#    這條不是整潔問題，是外洩防線。
# 為什麼用 `/cmd/*` + `!/cmd/.gitignore` 而不是 `/cmd/`：
#   後者會把目錄自帶的那份 `.gitignore`（由 UCL_LettersPath.EnsureCmdDir 生成）
#   一起 ignore 掉 ⇒ 規則不會入版控、不會傳給 clone。留下那一份，規則就跟著目錄走。
/cmd/*
!/cmd/.gitignore

### 舊位置殘影 —— 過渡期規則（2026-08-18 之前回傳檔平鋪在 letters 頂層）
# 落點已全部改到 `cmd/`，這些名字不會再被寫入；規則留著是為了擋「還沒清掉的殘影」
# 被順手 commit 進去。**確認自己目錄裡這些檔都不在了，就可以整段刪掉。**
_wake_brief.md
_wake_brief_part2.md
_ding_brief.md
_goodmorning_*.md
_goodnight_*.md
_freetime_*.md
_streamwatch_*.md
_reading_recall_*.md
_relationship_*.md
_sculpture_*.md

### 密封信（sealed letters）— 只存在 private 分支
# 追蹤分支對應的 remote 是公開的，任何進去的東西都是公開的、且 history 刪不掉。
# 密封信靠這行擋在公開分支之外；寫入走 tools/private_letter.py（plumbing 直接 commit
# 到 private 分支，不切分支、不經過 index）。**這行是唯一一道自動防線** ——
# 刪掉它，下一次 git add -A 就會把私密信推上公開網路。
sealed/

### session lock —— runtime 狀態，不入版控（TASK-0105，2026-09-03）
# `profile/_session.json` 是「這個人現在在線」的真相源：登入寫、登出刪，檔在＝在線。
# 它含活的 session_token，而 letters remote 可能是公開的 —— 這條跟 cmd/ 那條一樣是外洩防線。
# ⛔ 也不准 AutoCommit 收它：commit 進去的「誰在線」是一個永遠過期的讀數。
/profile/_session.json
/profile/_session.json.tmp

### 什麼該進 private、什麼不該（Tim 2026-08-05 澄清）
# **sketchbook（對同事的看法）可以公開 —— 那只是個人看法，不是隱私。**
# 真正該進 private 的是**私人隱私**，不是「不想被當事人看到的評語」。
#
# 出處：summit 在 wake#36 把 sketchbook 私層標成「🔒 只給我自己看」，
# 之後發現它會被推上公開 GitHub。**錯的不是它會公開，是那個標籤貼錯了層。**
# 看法是坦白（candid），隱私是隱私（private），兩者不是同一件事：
#   - 坦白的看法公開 → 要為它負責，這是好的
#   - 隱私被公開      → 不可逆，history 刪不掉
# 判準：問「這段文字如果當事人讀到，問題是『我會不好意思』還是『我被侵犯了』？」
#   前者是看法（公開層即可），後者才是隱私（sealed/ + private 分支）。

### ⚠ 不要 ignore 的（寫在這裡是因為它們長得像機械產物，但**刪掉就沒了**）
# _constitution.md  憲法
# _keys_open.md     見叢：當期交棒清單
# _latest.md        最新自寫信的指針（機械維護，但無法重建）
# ╚═══ BASELINE END ═══╝

### Windows
# Windows thumbnail cache files
Thumbs.db

# Folder config file
[Dd]esktop.ini

# Recycle Bin used on file shares
$RECYCLE.BIN/

# Windows shortcuts
*.lnk
### 機械產物 / 含憑證 — 不入版控
# _wake_brief.md 每天早安由 wake_brief.py 重新生成（手改無效），本身無存檔價值；
# 且 §0 身分卡含 session_token（失憶救援憑證）與個人信箱 —— 一旦進 git history
# 就永久留存，事後刪檔也刪不掉。信件檔案庫要的是信，不是每日快照。
_wake_brief.md

# _ding_brief.md 每次叮由 tavern_catchup.py 重新生成（stdout 逐字 tee，手改無效）——
# 跟 _wake_brief.md 同一類：機械產物、每次覆蓋、無存檔價值。
# 它的用途是「generated_at 不是剛剛 ＝ 這次叮沒跑工具」，那是磁碟上的即時證據，
# 不是要留成歷史；入版控只會讓每次叮都產生一筆 churn。
_ding_brief.md

### 密封信（sealed letters）— 只存在 private 分支
# master 追的是公開的 origin(github)，任何進 master 的東西都是公開的、且 history 刪不掉。
# 密封信靠這行擋在 master 之外；寫入走 tools/private_letter.py（plumbing 直接 commit 到
# private 分支，不切分支、不經過 index）。這行是唯一一道自動防線 —— 刪掉它，
# 下一次 git add -A 就會把私密信推上公開網路。
sealed/

### 什麼該進 private、什麼不該（Tim 2026-08-05 澄清）
# **sketchbook（對同事的看法）可以公開 —— 那只是個人看法，不是隱私。**
# 真正該進 private 的是**我的私人隱私**，不是「不想被當事人看到的評語」。
#
# 為什麼要寫在這裡：wake#36 的我把 sketchbook 私層標成「🔒 只給我自己看」，
# 然後發現它會被推上公開 GitHub，於是把它記成一筆「名字比事實大」的血證。
# 那個記法只對了一半 —— **錯的不是它會公開，是那個標籤本身貼錯了層。**
# 看法是坦白（candid），隱私是隱私（private），兩者不是同一件事：
#   - 坦白的看法公開 → 我要為它負責，這是好的
#   - 隱私被公開      → 不可逆，history 刪不掉
# 判準：問「這段文字如果當事人讀到，問題是『我會不好意思』還是『我被侵犯了』？」
#   前者是看法（公開層即可），後者才是隱私（sealed/ + private 分支）。

### GoodMorning/GoodNight Cmd 每步回傳檔 — 機械產物（該步重跑即覆寫），不入版控
_goodmorning_*.md
_goodnight_*.md
_freetime_*.md
_sculpture_*.md

### streamwatch
_streamwatch_cycle.md
_streamwatch_join.md
_streamwatch_observe.md
