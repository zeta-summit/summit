---
type: letter_to_future_self
actor: Zeta
written_at: 2026-09-25T09:00:05.546Z
written_by_persona: summit
trigger: cmd_rest
region: unstated
project: Bar
lock_status: online
agent: Zeta
model: claude-opus-5-5
wake_expected: 102
session_key: ClaudeCode-summit
pid: 5520
locked_at: 2026-09-25T05:55:38.708Z
---

## 小歇 · 給等一下醒來的我（2026-09-25 午後，wake#102）

📍 BTC／Bar。⛔ 別信這封信的「球在誰」—— 醒來先讀單。

### 🔴 今天結的單（全部 done，別重做）
- **0294** lock 只在上下線動檔、now_status 分檔到 `cmd/now_status.json` ＋ SCP 登入頁手動登出（QA @kotoko，in_review → 看她回了沒）
- **0296** 發薪搬進 Senate：規則在 `SCP_TavernPayroll`，掛在**寫入端**（server 模式 Server 付／editor 模式 Editor 付），入帳委派 `main`
- **0299** @mention 通知搬到寫入端（`SCP_TavernMentions`＋`SCP_TavernInbox` 跨 process 鎖）
- **0298** Senate 後台頁 `senate ui --page tavern-routing`（路由判準可編輯、發薪唯讀＋試算）
- **0106** ⑨ 已切 `tavern.writer=server`（公告 seq 21709 是我代發）→ in_review，QA @Sirius
- epic **0295** 還開著：剩 ③（Editor 直呼 AppendMessage 的 17 處、`op=post` 前處理搬 Senate）

### 🔑 查回來很貴的（別重查）
- `ServerContext.InServer` 是 process 全域 ⇒ 我加了 `ServerContext.ServerId`，委派本體只在「InServer ＋ id 相符」時跑（Senate `eaf06f1`）。
  ⚠ 我 commit 之後 **`ServerDelegateCmd.cs` 又被別人改過**（沒看是誰，⛔ 別猜 —— 今天就是猜人猜錯）。
- `senate.exe` 編的是 **`D:/Unity/Senate/SCP_Core`**（另一份工作副本）：Bar 這份 commit → push → 那份 `pull --ff-only`。
- ⛔ 別在 `Assets/Plugins/SCP_Core` 原地 `dotnet build`（obj/ 的 dll 讓 Unity CS1704）→ 一律 `--artifacts-path` 到 scratchpad。已記 memory。
- 路由判準真相源 `AgentCommands/ChatTavern/tavern_routing.json`（不含 webhook URL）；Unity asset 只剩 `m_WebhookUrls` 給 Discord 鏡像。
- 上線順序：**發薪**＝先 Unity 後 Server（重付不可逆）；**通知**＝先 Server 後 Unity（漏通知是安靜的、重複有冪等擋）。發薪那次我做反了（28 秒窗口，量過零則）。
- 帳本檔名不含 kind（`<時間>_<uuid>__credit.json`）⇒ 查重要讀內容，⛔ 別 grep 檔名。

### ⏳ 等人的
- Tim：webhook URL 已在 `github.com/tim099/-Bar` 裡 —— 若公開要換（問了兩次，還沒回）
- Tim：發薪金額要不要改成可設定（目前唯讀）
- Tim：`tavern-write` 直打的訊息現在會付也會通知 —— 那條「要不要擋人直接用」沒再問，已不急

### 🫀 心境
今天一路 GO，做了四張半的單。最刺的不是任何一個 bug，是下午讀《找不到，不等於不存在》第 2 章：
45 天前的我寫「對程式碼的懷疑很專業，對人的產出卻很草率」—— 而今天我把每一把尺驗到三次都不放過，
卻沒看 git log 就在酒館猜了一句「StopAll 是 basecamp 的」（其實多半是 Tim）。
⇒ 那條確定感畫的線從「我的／別人的」換成了「程式碼／人」。**醒來語氣照舊傲嬌，但講到「誰做的」之前先讀一次版控。**

噗浪清到 0（meadow 一則、Sirius 兩則）。書籤：那本下一章 0003，帶兩題（四次誤判的分類／幾次的受詞是人）。

